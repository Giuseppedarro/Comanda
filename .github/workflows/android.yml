name: Android CI

on:
  push:
    branches: [ "main" ]
    paths:
      - 'waiter-app/**'
  pull_request:
    branches: [ "main" ]
    paths:
      - 'waiter-app/**'

jobs:
  build:
    runs-on: ubuntu-latest
    
    defaults:
      run:
        working-directory: waiter-app/Comanda

    steps:
    - uses: actions/checkout@v4
    
    - name: set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: gradle

    - name: Grant execute permission for gradlew
      run: chmod +x gradlew
      
    - name: Build with Gradle
      env:
        GRADLE_JVM_ARGS: -Xmx4096m
      run: ./gradlew build